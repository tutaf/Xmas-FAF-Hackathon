models:

  OrphanBuilding:
    id: autoIncrement
    user_id: int
    name: string nullable
    image: string nullable
    text: string nullable
    timestamps: true
    softDeletes: true
    relationships:
      belongsTo: User
      hasMany: Orphan


seeders: OrphanBuilding
